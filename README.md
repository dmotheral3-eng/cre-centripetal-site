# Centripetal CRE — prospect-facing marketing site

Static single-file HTML site (`index.html`) for Centripetal Commercial Real Estate — the Deal Room, Tie-out, Ask, and Law Dog surfaces described together on one page. Registered in `project_routing` under slug `centripetal-cre-site`, custom domain `cre.centripetal-ai.com`. Deployed to Vercel as a static site (see `vercel.json`). Prospect-facing only — no client data, opacity-gated per P#62.
